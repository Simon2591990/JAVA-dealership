package vehicles;

public class Car extends Vehicle {

    public Car(double price, String color, Engine engine, Tyres tyres, VehicleType vehicleType) {
        super(price, color, engine, tyres, vehicleType);
    }
}
